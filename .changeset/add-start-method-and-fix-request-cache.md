---
'@korix/kori': patch
---

- Add `start()` shortcut method to Kori instance for easier initialization.
- Fix request body caching logic to prevent incorrect reuse of parsed JSON.
- Improve TSDoc comments across all modules.
