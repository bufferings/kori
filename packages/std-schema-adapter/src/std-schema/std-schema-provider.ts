/**
 * Provider identifier for Standard Schemas in the Kori framework.
 */
export const STANDARD_SCHEMA_PROVIDER = 'standard-schema';

/**
 * Type representing the Standard Schema provider identifier.
 */
export type KoriStdSchemaProvider = typeof STANDARD_SCHEMA_PROVIDER;
